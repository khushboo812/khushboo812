#include<stdio.h>
int main() {
    int side, area;;
    printf("enter the side of the sqaure");
    scanf("%d",&side);
    area = side*side;
    printf("area of the sqaure is %d", area);
    return 0;
}